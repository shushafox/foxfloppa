set {Autoload.NpcsTalkedTo["Evil"]} = true
set {Counter} += 1
set {Autoload.GlobalCounter} -= 1
Aboba
Blobeba
if {Autoload.IsDay} == true:
	Switch to night?
	- Yes
		set {Autoload.IsDay} = false
	- No
else:
	Switch to day?
	- Yes
		set {Autoload.IsDay} = true
	- No
